---
number headings: auto, first-level 1, max 3, 1.1
---

# 1 衡量指标

通常只考虑最差情况下的开销，即O（读作：big o）

## 1.1 时间复杂度
## 1.2 空间复杂度

需要额外占用的存储空间

## 1.3 常用操作

### 1.3.1 异或操作

常见题目：

* 在给定数组中，有一个整数的个数为奇数，其它整数的个数均为偶数，比如：[1, 1, 3, 6, 8, 8, 6]。请问个数为奇数的整数是多少？

```js
function find(arr) {
    let num = 0
    for (let i=0; i < arr.length; i++) {
        num ^= arr[i]
    }
    return num
}
```

* 在给定的数组中，有两个整数的个数为奇数，其它整数的个数均为偶数，比如：[1, 1, 3, 6, 5, 8, 8, 6]。请问个数为整数的两个数值分别是？

```js
```

### 1.3.2 中值操作

# 2 排序算法

## 2.1 冒泡排序、选择排序

* 时间复杂度：O($N^2$)
* 空间复杂度：O(N)


## 2.2 插入排序

## 2.3 快速排序

### 2.3.1 荷兰国旗问题

#### 问题1.

给定一个数组arr，和一个数num，请把小于等于num的数放在数组的左边，大于num的数放在数组的右边。要求额外空间复杂度0(1)，时间复杂度0(N)

```js
```

#### 问题2.

给定一个数组arr，和一个数num，请把小于num的数放在数组的左边，等于num的数放在数组的中间，大于num的数放在数组的右边。要求额外空间复杂度0(1)，时间复杂度0(N)

```js
```
